rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{projectId}/{allPaths=**} {
      // Allow read/write for authenticated users who own or collaborate on the project
      allow read, write: if request.auth != null;
    }
  }
}
